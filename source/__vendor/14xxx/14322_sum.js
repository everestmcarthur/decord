// Module ID: 14322
// Function ID: 14323
// Name: sum
// Dependencies: [14307]

// Module 14322 (sum)
import call from "call" /* 14307 */;

let c0 = 0;
let closure_1 = Math.random();
let closure_2 = call(1.toString);

export default (arg0) => {
  let str = "";
  if (undefined !== arg0) {
    str = arg0;
  }
  const sum = c0 + 1;
  c0 = sum;
  return `Symbol(${str}` + ")_" + callback(sum + closure_1, 36);
};
