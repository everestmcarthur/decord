// Module ID: 11685
// Function ID: 11686
// Name: MarkupReactLinkUtils
// Dependencies: [8435, 8039, 2]
// Exports: isLinkTrusted

// Module 11685 (MarkupReactLinkUtils)
import MaskedLinkUtils from "MaskedLinkUtils" /* 8435 */;
import size from "module_2" /* 2 */;

const result = size.fileFinishedImporting("modules/markup/MarkupReactLinkUtils.tsx");

export const isLinkTrusted = function isLinkTrusted(target) {
  if (null == target.target) {
    return tmp;
  } else {
    MaskedLinkUtils;
    if (null == target.title) {
      let title = tmp2(8039).astToString(target.content);
      tmp5(tmp6, title);
      const tmp2Result = tmp2(8039);
    }
    title = target.title;
    tmp2 = require;
  }
};
