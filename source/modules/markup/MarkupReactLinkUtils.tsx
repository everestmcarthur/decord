// Module ID: 11707
// Function ID: 11708
// Name: MarkupReactLinkUtils
// Dependencies: [8456, 8061, 2]
// Exports: isLinkTrusted

// Module 11707 (MarkupReactLinkUtils)
import MaskedLinkUtils from "MaskedLinkUtils" /* 8456 */;
import size from "module_2" /* 2 */;

const result = size.fileFinishedImporting("modules/markup/MarkupReactLinkUtils.tsx");

export const isLinkTrusted = function isLinkTrusted(target) {
  if (null == target.target) {
    return tmp;
  } else {
    MaskedLinkUtils;
    if (null == target.title) {
      let title = tmp2(8061).astToString(target.content);
      tmp5(tmp6, title);
      const tmp2Result = tmp2(8061);
    }
    title = target.title;
    tmp2 = require;
  }
};
