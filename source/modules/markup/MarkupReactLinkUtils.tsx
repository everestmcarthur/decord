// Module ID: 11709
// Function ID: 11710
// Name: MarkupReactLinkUtils
// Dependencies: [8458, 8062, 2]
// Exports: isLinkTrusted

// Module 11709 (MarkupReactLinkUtils)
import MaskedLinkUtils from "MaskedLinkUtils" /* 8458 */;
import size from "module_2" /* 2 */;

const result = size.fileFinishedImporting("modules/markup/MarkupReactLinkUtils.tsx");

export const isLinkTrusted = function isLinkTrusted(target) {
  if (null == target.target) {
    return tmp;
  } else {
    MaskedLinkUtils;
    if (null == target.title) {
      let title = tmp2(8062).astToString(target.content);
      tmp5(tmp6, title);
      const tmp2Result = tmp2(8062);
    }
    title = target.title;
    tmp2 = require;
  }
};
