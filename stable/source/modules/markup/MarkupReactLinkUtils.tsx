// Module ID: 11619
// Function ID: 11620
// Name: MarkupReactLinkUtils
// Dependencies: [8371, 7988, 2]
// Exports: isLinkTrusted

// Module 11619 (MarkupReactLinkUtils)
import MaskedLinkUtils from "MaskedLinkUtils" /* 8371 */;
import size from "module_2" /* 2 */;

const result = size.fileFinishedImporting("modules/markup/MarkupReactLinkUtils.tsx");

export const isLinkTrusted = function isLinkTrusted(target) {
  if (null == target.target) {
    return tmp;
  } else {
    MaskedLinkUtils;
    if (null == target.title) {
      let title = tmp2(7988).astToString(target.content);
      tmp5(tmp6, title);
      const tmp2Result = tmp2(7988);
    }
    title = target.title;
    tmp2 = require;
  }
};
