// Module ID: 11863
// Function ID: 11864
// Name: MarkupReactLinkUtils
// Dependencies: [8640, 8244, 2]
// Exports: isLinkTrusted

// Module 11863 (MarkupReactLinkUtils)
import MaskedLinkUtils from "MaskedLinkUtils" /* 8640 */;
import size from "module_2" /* 2 */;

const result = size.fileFinishedImporting("modules/markup/MarkupReactLinkUtils.tsx");

export const isLinkTrusted = function isLinkTrusted(target) {
  if (null == target.target) {
    return tmp;
  } else {
    MaskedLinkUtils;
    if (null == target.title) {
      let title = tmp2(8244).astToString(target.content);
      tmp5(tmp6, title);
      const tmp2Result = tmp2(8244);
    }
    title = target.title;
    tmp2 = require;
  }
};
