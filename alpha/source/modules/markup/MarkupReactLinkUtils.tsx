// Module ID: 11854
// Function ID: 11855
// Name: MarkupReactLinkUtils
// Dependencies: [8602, 8206, 2]
// Exports: isLinkTrusted

// Module 11854 (MarkupReactLinkUtils)
import MaskedLinkUtils from "MaskedLinkUtils" /* 8602 */;
import size from "module_2" /* 2 */;

const result = size.fileFinishedImporting("modules/markup/MarkupReactLinkUtils.tsx");

export const isLinkTrusted = function isLinkTrusted(target) {
  if (null == target.target) {
    return tmp;
  } else {
    MaskedLinkUtils;
    if (null == target.title) {
      let title = tmp2(8206).astToString(target.content);
      tmp5(tmp6, title);
      const tmp2Result = tmp2(8206);
    }
    title = target.title;
    tmp2 = require;
  }
};
